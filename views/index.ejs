<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <header>
    <div class="container">
      <h1><i class="fas fa-sitemap"></i> Sitemap Generator</h1>
      <p>Generate XML sitemaps for your website quickly and easily</p>
    </div>
  </header>

  <main class="container">
    <section class="card">
      <h2>Generate Your Sitemap</h2>
      <form action="/generate" method="POST">
        <div class="form-group">
          <label for="url">Website URL</label>
          <div class="input-group">
            <input type="text" id="url" name="url" placeholder="example.com" required>
          </div>
        </div>
        
        <div class="form-group">
          <label for="maxPages">Maximum Pages (optional)</label>
          <input type="number" id="maxPages" name="maxPages" placeholder="Default: 50" min="1" max="500">
          <small>Limit the number of pages to crawl (max 500)</small>
        </div>
        
        <div class="form-group checkbox">
          <input type="checkbox" id="includeImages" name="includeImages">
          <label for="includeImages">Include images in sitemap</label>
        </div>
        
        <button type="submit" class="btn btn-primary">
          <i class="fas fa-cog"></i> Generate Sitemap
        </button>
      </form>
    </section>

    <% if (error) { %>
      <section class="card error-card">
        <h2><i class="fas fa-exclamation-triangle"></i> Error</h2>
        <p><%= error %></p>
      </section>
    <% } %>

    <% if (result) { %>
      <section class="card result-card">
        <div class="result-header">
          <h2><i class="fas fa-check-circle"></i> Sitemap Generated</h2>
          <p>Found <strong><%= result.count %></strong> URLs on <strong><%= result.domain %></strong></p>
        </div>
        
        <div class="xml-container">
          <div class="xml-header">
            <h3>XML Sitemap</h3>
            <div class="xml-actions">
              <button id="copyBtn" class="btn btn-sm">
                <i class="fas fa-copy"></i> Copy
              </button>
              <button id="downloadBtn" class="btn btn-sm">
                <i class="fas fa-download"></i> Download
              </button>
            </div>
          </div>
          <pre id="xmlContent"><%= result.xml %></pre>
        </div>
        
        <div class="instructions">
          <h3>What to do with your sitemap?</h3>
          <ol>
            <li>Download the XML file and upload it to your website's root directory</li>
            <li>Submit the sitemap URL to search engines:
              <ul>
                <li><a href="https://search.google.com/search-console" target="_blank">Google Search Console</a></li>
                <li><a href="https://www.bing.com/webmasters/home" target="_blank">Bing Webmaster Tools</a></li>
              </ul>
            </li>
            <li>Add the sitemap location to your robots.txt file:
              <pre>Sitemap: https://<%= result.domain %>/sitemap.xml</pre>
            </li>
          </ol>
        </div>
      </section>
    <% } %>
  </main>

  <footer>
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> Sitemap Generator | Built with Node.js</p>
    </div>
  </footer>

  <script src="/js/main.js"></script>
</body>
</html> 